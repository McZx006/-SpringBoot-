<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.platform.mapper.XueyuanMapper">
    <sql id="whereSql">
        <where>
            <if test="xuehao != null and xuehao != ''">
                AND x.xuehao LIKE CONCAT('%', #{xuehao}, '%')
            </if>
            <if test="name != null and name != ''">
                AND x.name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
    </sql>

    <select id="page" resultType="map">
        SELECT x.id, x.user_id AS userId, x.xuehao, x.name, x.gender, x.phone,
               x.email, x.avatar, x.addtime, u.username, u.status
        FROM t_xueyuan x
        LEFT JOIN t_users u ON u.id = x.user_id
        <include refid="whereSql"/>
        ORDER BY x.addtime DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM t_xueyuan x
        LEFT JOIN t_users u ON u.id = x.user_id
        <include refid="whereSql"/>
    </select>

    <select id="findById" resultType="map">
        SELECT x.id, x.user_id AS userId, x.xuehao, x.name, x.gender, x.phone,
               x.email, x.avatar, x.addtime, u.username, u.status
        FROM t_xueyuan x
        LEFT JOIN t_users u ON u.id = x.user_id
        WHERE x.id = #{id}
    </select>

    <select id="findByUserId" resultType="map">
        SELECT x.id, x.user_id AS userId, x.xuehao, x.name, x.gender, x.phone,
               x.email, x.avatar, x.addtime, u.username, u.status
        FROM t_xueyuan x
        LEFT JOIN t_users u ON u.id = x.user_id
        WHERE x.user_id = #{userId}
        LIMIT 1
    </select>

    <select id="findByXuehao" resultType="map">
        SELECT x.id, x.user_id AS userId, x.xuehao, x.name, x.gender, x.phone,
               x.email, x.avatar, x.addtime, u.username, u.status
        FROM t_xueyuan x
        LEFT JOIN t_users u ON u.id = x.user_id
        WHERE x.xuehao = #{xuehao}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.example.platform.entity.Xueyuan" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_xueyuan (user_id, xuehao, name, gender, phone, email, avatar)
        VALUES (#{userId}, #{xuehao}, #{name}, #{gender}, #{phone}, #{email}, #{avatar})
    </insert>

    <update id="update" parameterType="com.example.platform.entity.Xueyuan">
        UPDATE t_xueyuan
        <set>
            <if test="xuehao != null">xuehao = #{xuehao},</if>
            <if test="name != null">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteBatch">
        DELETE FROM t_xueyuan
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
