<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.platform.mapper.UserMapper">
    <select id="findByUsernameAndRole" resultType="com.example.platform.entity.User">
        SELECT id, username, password, role, status, addtime
        FROM t_users
        WHERE username = #{username}
          AND role = #{role}
        LIMIT 1
    </select>

    <select id="findById" resultType="com.example.platform.entity.User">
        SELECT id, username, password, role, status, addtime
        FROM t_users
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.platform.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_users (username, password, role, status)
        VALUES (#{username}, #{password}, #{role}, #{status})
    </insert>

    <update id="updatePassword">
        UPDATE t_users
        SET password = #{password}
        WHERE id = #{id}
    </update>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM t_users
    </select>
</mapper>
